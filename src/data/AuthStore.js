import {ReduceStore} from 'flux/utils';
import {AppActionTypes} from './AppActionTypes';

export class AuthStore extends ReduceStore {
  getInitialState() {
    return null;
  }

  reduce(state, action) {
    return state;
  }
}
